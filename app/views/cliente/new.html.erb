
<div class="container-div" >
	<h1 align="center" >Nuevo Cliente</h1>
	<%= form_for @cliente, :url => { :action => :create, :validate => true } do |f| %>
	<%= render "form", f: f %>
	<% end %>
</div>
